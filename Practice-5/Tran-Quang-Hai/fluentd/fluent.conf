<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match *.**>
  @type copy
  <store>
    @type elasticsearch
    host tuandat82.vddns.vn
    port 9200
    index_name haitranquang_official
    logstash_format true
    logstash_prefix haitranquang_official
    include_tag_key true
    flush_interval 10s
  </store>
  <store>
    @type stdout
  </store>
</match>